---
title: "[Future] タイムスライダー(出発時刻を連続的に動かす)"
labels: type:future
milestone:
blockedBy:
---
## 概要(プレースホルダ — 実装計画なし)

出発時刻をスライダーで動かすと到達圏がリアルタイムに変化する UI。時間帯による到達範囲の変化(昼 vs 深夜)を直感的に見せる。

## メモ

- 探索 <200ms + ポリゴン生成の性能次第。プロファイル(#14, #19 の実測)を見て、プリ計算(時刻ステップごとのキャッシュ)か逐次計算かを判断
- Worker のクエリキャンセル機構(#15)が前提
